#include "Logger.h"
#include "myglobals.h"

//std::ofstream HyperSpectralToolbox::Logger::out_stream;

/*
void stream_to_log(std::string theStream)
{
	//using namespace HyperSpectralToolbox; 
	auto sp_ofstream = HyperSpectralToolbox::Logger::openStream(); 
	(*sp_ofstream) << theStream << "\n";	
	sp_ofstream->close(); 
}	
*/